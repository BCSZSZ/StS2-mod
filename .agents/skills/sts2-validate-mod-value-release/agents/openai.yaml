interface:
  display_name: "Validate StS2 Value Release"
  short_description: "Gate, validate, and explicitly deploy runtime values"
  default_prompt: "Use $sts2-validate-mod-value-release to prove an authorized value diff and deploy it only when the user explicitly requests the active local mod update."
