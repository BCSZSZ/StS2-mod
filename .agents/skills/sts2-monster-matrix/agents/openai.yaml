interface:
  display_name: "StS2 Monster Intent Data"
  short_description: "Audit monster intent probabilities and matrices"
  default_prompt: "Use $sts2-monster-matrix to trace and validate source-backed monster intent transitions while keeping deterministic pressure matrices diagnostic-only."
